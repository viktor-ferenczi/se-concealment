<?xml version="1.0"?>
<PluginData xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="GitHubPlugin">

    <!-- The ID of your plugin, never change this -->
    <Id>C88741F0-0AA5-4263-BD78-5D21467A84D3</Id>

    <!-- GitHub repository ID -->
    <RepoId>viktor-ferenczi/se-concealment</RepoId>

    <!-- The name of your plugin that will appear in the plugin list -->
    <FriendlyName>Concealment</FriendlyName>

    <!-- The author name that you want to appear in the plugin list -->
    <Author>Viktor</Author>

    <!-- Optional tooltip for the plugin in the plugin list -->
    <Tooltip>Stops the simulation of grids if far away from all online players</Tooltip>

    <!-- Optional plugin description. If omitted, this will be the same as the Tooltip. 1000 characters max. -->
    <Description>
        Concealment is a server-side Space Engineers plugin for Magnetar that reduces
        dedicated server load by skipping work that only matters on a graphical client.

        It uses Harmony patches — for example disabling LCD text-panel visibility
        updates on the dedicated server — to cut needless CPU and network overhead.
        Patches are guarded by startup code-change detection so they fail-safe when a
        Space Engineers update alters the patched methods, and the plugin is configured
        through the Magnetar PluginSdk with its UI rendered by Quasar.

        Functionality is inspired by and reimplements the original Torch plugin
        Concealment by TorchAPI: https://github.com/TorchAPI/Concealment
    </Description>

    <!-- Only source code in the Directory entries listed in SourceDirectories is compiled. Exclude any test projects or similar. -->
    <SourceDirectories>
        <Directory>ServerPlugin</Directory>
        <Directory>Shared</Directory>
    </SourceDirectories>

    <!-- The current plugin version's commit hash -->
    <Commit>TODO Git commit hash</Commit>

</PluginData>